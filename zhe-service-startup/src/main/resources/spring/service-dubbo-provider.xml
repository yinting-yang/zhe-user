<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
        http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    <!-- 扫描注解包路径，多个包用逗号分隔，不填pacakge表示扫描当前ApplicationContext中所有的类 -->
    <dubbo:annotation package="com.zhe.user"/>
    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="service-user"/>
    <!-- 使用zookeeper广播注册中心暴露服务地址 -->
    <dubbo:registry protocol="zookeeper" address="${dubbo.zookeeper.url}"/>

    <!-- 用dubbo协议在21881端口暴露服务,服务端集群,不同的server不同的端口 -->
    <dubbo:protocol name="dubbo" port="${duboo.service.export.port}"/>

    <!-- 声明需要暴露的服务接口 -->
    <!--服务商-start-->
    <!--<dubbo:service interface="com.lincomb.lbg.cooperation.service.IFacilitatorService" ref="facilitatorService" timeout="10000" />
    <dubbo:service interface="com.lincomb.lbg.cooperation.service.IFacilitatorDemandService" ref="facilitatorDemandService" timeout="10000" />-->
    <!--服务商-end-->


</beans>
